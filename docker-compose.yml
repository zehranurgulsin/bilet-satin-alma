version: "3.9"
services:
  app:
    build: .
    container_name: yavuzlar-app
    ports:
      - "8080:80"       
    environment:
      - TZ=Europe/Istanbul
      - APP_ENV=prod
    volumes:
      - yavuzlar_data:/var/www/html/data   
    restart: unless-stopped

volumes:
  yavuzlar_data:
